<% if(field.present? && field.to_s.class != NilClass) %>
<%
	require 'rqrcode'
	qrcode = RQRCode::QRCode.new(field.to_s)
	svg = qrcode.as_svg
	IO.write("#{Rails.root}/public/system/#{field.to_s}.svg", svg.to_s)
%>

	<img src='<%= "/system/#{field.to_s}.svg" %>'></img>
	

<% end %>