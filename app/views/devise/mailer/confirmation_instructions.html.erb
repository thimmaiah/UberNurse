<p>Welcome <%= @resource.first_name %>!</p>

<p>
	Please confirm your email by clicking on the link below:
</p>

<table border="0" cellpadding="0" cellspacing="0" class="btn btn-primary">
  <tbody>
    <tr>
      <td align="left">
        <table border="0" cellpadding="0" cellspacing="0">
          <tbody>
            <tr>
              <td> 
              <%= link_to 'Confirm my account', 
					confirmation_url(@resource, confirmation_token: @token, redirect_url: ENV['REDIRECT_SUCCESSFULL_EMAIL_VERIFICATION']),
					class: "btn btn-primary" %> 
			  </td>
            </tr>
          </tbody>
        </table>
      </td>
    </tr>
  </tbody>
</table>


<% if @resource.role == "Care Giver" || @resource.role == "Nurse" %>

	<p>
		Please also verify that your the following details are accurate as they are required for payments
	</p>

	<p>Bank Account: <%=@resource.bank_account%> </p>
	
	<p>Sort Code: <%=@resource.sort_code%> </p>

<% end %>